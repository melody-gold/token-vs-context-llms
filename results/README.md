# Results

Store generated experiment outputs here.

Examples:

- layerwise probe metrics in JSON
- plots of `R^2`, cosine similarity, and MSE by layer
- short experiment summaries comparing configurations
