# Artifacts

Store extracted token-level activations here.

Expected files:

- `.npz` activation tensors produced by `token-vs-context extract`
- adjacent `.json` metadata files describing the extraction configuration
